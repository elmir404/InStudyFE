<div class="main-content app-content mt-0">
    <div class="side-app">

        <!-- CONTAINER -->
        <div class="main-container container-fluid">

            <!-- PAGE-HEADER -->
            <div class="page-header">
                <h1 class="page-title">Update Duration</h1>
                <div>
                    <ol class="breadcrumb">
                        <li class="breadcrumb-item"><a href="javascript:void(0)">Pages</a></li>
                        <li class="breadcrumb-item active" aria-current="page">Update Duration</li>
                    </ol>
                </div>
            </div>
            <!-- PAGE-HEADER END -->
            <!-- ROW-1 OPEN -->
            <div class="row">
                <div class="col-lg-12">
                    <form id="form" class="card">
                        <div class="card-header">
                            <div class="card-title">Update Duration</div>
                        </div>
                        <div id="durationForm" class="card-body">

                            <!--End Row-->
                        </div>
                        <div class="card-footer">
                            <!--Row-->
                            <div class="row">
                                <div class="col-md-3"></div>
                                <div class="col-md-9">
                                    <a id="updateDuration" class="btn btn-primary">Update Duration</a>
                                </div>
                            </div>
                            <!--End Row-->
                        </div>
                    </form>
                </div>
            </div>
            <!-- /ROW-1 CLOSED -->
        </div>
        <!-- CONTAINER CLOSED -->
    </div>
</div>
@section Scripts {
    <script src="~/AdminFiles/js/update-duration.js"></script>
    <script>
        $(document).ready(function () {
            $(document).on('change', '#University', function () {
                debugger;
                $("#form").data("changed", true);
            });
            $(document).on('change', '#EducationProgram', function () {
                debugger;
                $("#form").data("changed", true);
            });
            $(document).on('change', '#Speciality', function () {
                debugger;
                $("#form").data("changed", true);
            });
            $(document).on('change', '#DurationDate', function () {
                debugger;
                $("#form").data("changed", true);
            });
            $(document).on('change', '#enDescription', function () {
                debugger;
                $("#form").data("changed", true);
            });
            $(document).on('change', '#ruDescription', function () {
                debugger;
                $("#form").data("changed", true);
            }); $(document).on('change', '#files', function () {
                debugger;
                $("#form").data("changed", true);
            });
        var isClicked = false;
        $('#updateDuration').on('click', function (evt) {
            debugger;
            isClicked = true;
        });
        debugger;

        window.addEventListener("beforeunload", function (e) {
            var confirmationMessage = 'It looks like you have been editing something. '
                + 'If you leave before saving, your changes will be lost.';

        
            debugger;
            if ($("#form").data("changed") && isClicked == false) {
                (e || window.event).returnValue = confirmationMessage; //Gecko + IE
                return confirmationMessage;

            }

            //Gecko + Webkit, Safari, Chrome etc.
        });
        });






    </script>
        }
